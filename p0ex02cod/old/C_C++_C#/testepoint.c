#include <stdio.h>
#include<stdlib.h>

typedef struct
{
    int *lista;
    int size;
} VETOR;

int main(){
	VETOR *lista;
	VETOR size;
	printf("%p\n", size);
	//printf("%p\n", lista);
	//printf("%p\n", *lista);
	//printf("%p\n", &lista);
	printf("%p\n", *VETOR);
	//printf("%p\n", size);
}


VETOR * ordena(VETOR *a) {
    
    return NULL;
}
