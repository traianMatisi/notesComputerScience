Algoritmo "sintaxeInput1a"
//
//usamos os 4 tipos primitivos
//recebemos INPUTS através do comando LEIA
//fornecemos OUTPUTS através do comando ESCREVA e ESCREVAL
Var
   a : inteiro
   b : real
   c : logico
   d : caractere
Inicio
   d := "Tudo bem aí?"
   escreva("Digite valores inteiros para a variável A: ")
   leia(a)
   escreval("Valor digitado: ", a)
   escreval(a, b, c, d)
Fimalgoritmo